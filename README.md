## SAVICS EMR Testing project


### To run the project
 #### Create the virtual env and install requirement
 `python -m venv .env`<br/>
    <br/>
 `source ./env/bin/activate`<br/>
    <br/>
 `python -m pip install -r requirements.txt`


 #### Run the server  
`./runserver` or `bash runserver`